import React from "react";
import Footer from "../Footer";
import Navbar from "../Navbar";

const About = ({ path }) => {
  return (
    <>
      <Navbar path={path} />
      <Footer />
    </>
  );
};

export default About;
