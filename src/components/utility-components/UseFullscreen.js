import React from "react";
import styled from "styled-components";

const UseFullscreen = () => {
  return <AlertContainer></AlertContainer>;
};

export default UseFullscreen;

const AlertContainer = styled.div``;

const XButton = styled.button``;
